#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(){
    /*
    scanf(); ## To pull from the input buffer
    printf(); ## To push to the console
    */
    // Declare basic int variables
    int t, r, c, i;
    // Import number of test cases from the buffer
    scanf("%d", t);
    // Enter for loop for amount of tests
    for(i = 0; i < t; i++){
        // Import the size of the grid (r and c) for Rows and Columns
        scanf("%d%d", r, c);
        // Import segments on board as a start and end point then compute if horizontal intersects with a vertical

    }
}